def ajouter_lettre(T, lettre):
    taille = len(T)
    # créer un nouveau tableau de taille len(T) + 1
    # rempli de 0

    # remplir ce tableau grâce à un parcours de T
    # sur les indices

    # rajouter la lettre à la dernière position 
    # dans le nouveau tableau (len(T))

    # renvoyer le nouveau_tableau
    pass

T1 = ['X', 'F', '+']
lettre = '+'
T2 = ajouter_lettre(T1, lettre)
print(T2)